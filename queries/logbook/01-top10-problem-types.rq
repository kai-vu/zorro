# Which problem types appear most frequently in the logbook extracts?

PREFIX : <https://w3id.org/zorro#>
PREFIX rdf: <http://www.w3.org/1999/02/22-rdf-syntax-ns#>
PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>

SELECT ?problemType (COUNT(?problemType) AS ?problemTypeFrequency)
WHERE {
    ?problem rdf:type ?problemType .
    ?problemType rdfs:subClassOf :Problem .
}
GROUP BY ?problemType
ORDER BY DESC(?problemTypeFrequency)
LIMIT 10
